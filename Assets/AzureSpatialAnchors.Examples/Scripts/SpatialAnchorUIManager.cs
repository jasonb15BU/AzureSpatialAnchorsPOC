using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpatialAnchorUIManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    //when spawnedObject doesn't equal null, we can make the template visible and take in inputs.
    /*
    [SerializeField]
    private GameObject tempMenu
    //subscribe to the spawnedObject event (?)
    void activateMenu()
    {
        
    } 
    */

    // Update is called once per frame
    void Update()
    {
        
    }
}
